# ── Secrets & environment ──
.env
.env.*
!.env.example
resend_config.txt
admin-auth.json

# ── Database (data lives on the server, not in git) ──
brain.db
brain_backup.db
*.sqlite
*.sqlite3

# ── Supabase backups (contain customer PII — never commit) ──
supabase_backup/

# ── Node ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS / editor junk ──
.DS_Store
*.log
Thumbs.db

# ── Python ──
__pycache__/
*.pyc

# ── Local snapshots / backups (do not deploy) ──
*_backup*.html
*_snapshot*.html
*_monolithic*.html
index_backup_before_refine_*.html

# ── Handoff credentials (NHẠY CẢM — chỉ local) ──
CREDENTIALS.local.md

# ── Agent runtime state ──
notify_config.json
mcp_state.json

# ── Generated vault markdown (từ brain.db) ──
vault-ready/
context-files/
